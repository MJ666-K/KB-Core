# Knowledge Core 分享大纲 + 流程图

> 建议时长 45 分钟 | 跨团队技术分享

---

## 分享大纲

### Slide 1 — 封面（1 min）

- 标题：**Knowledge Core —— 基于 Agent 的智能知识库**
- 副标题：让 LLM 自己决定怎么回答你的问题
- 你的名字 / 团队 / 日期

---

### Slide 2-3 — 开场 Demo（5 min）

**不要放 slides，直接开系统**

Demo 1：事实问答
```
问："Kubernetes 的 Pod 是什么？"
→ 展示：检索过程 → 带 [1][2] 引用的回答 → 来源文档链接
```

Demo 2：对比分析
```
问："对比 Docker Compose 和 Kubernetes 在本地开发场景的优劣"
→ 展示：Agent 选择 compare skill → 分别检索 → 对比表格
```

Demo 3（可选）：闲聊
```
问："你好"
→ 展示：Agent 判断不需要检索 → 直接回答
```

> 💡 先 Demo 再讲原理。大家会带着"怎么做到的"好奇心听后面。

**Backup 计划**：提前录屏，现场挂了切视频。

---

### Slide 4 — 痛点：知识管理的三个难题（3 min）

```
📄 文档散落各处，找信息靠缘分
🔑 换个表述就搜不到（关键词搜索的局限）
🧩 跨文档的对比、综合推理需要人肉逐篇翻
```

---

### Slide 5 — 传统 RAG vs 我们的方案（3 min）

| | 传统 RAG | KB-Core |
|--|---------|---------|
| 流程 | 固定管线：检索→生成 | Agent 自主编排 |
| 路由 | if/else 关键词匹配 | LLM 语义理解 |
| 扩展 | 加功能改代码 | 加一个 Markdown 文件 |
| 复合问题 | 无法处理 | 自动组合多个 Skill |

**一句话**：传统 RAG 是流水线工人，我们是项目经理。

---

### Slide 6 — 分层架构（3 min）

```
Agent（LLM 自主决策）
  └── Skill（Markdown 驱动，固化最佳实践）
        └── Tool（原子操作，可独立复用）
              └── Data（PostgreSQL + pgvector）
```

> 核心原则：Agent 层灵活，Skill 层固化

---

### Slide 7-8 — Agent 层详解（7 min）

**Agent Loop 四步循环**：

```
THINK → CALL → OBSERVE → DECIDE
                         ↓
                    继续？→ 回到 THINK
                    完成？→ 返回答案
```

**三种终止路径**（设计亮点，值得展开）：

| 路径 | 什么时候触发 | 怎么处理 | 为什么 |
|------|------------|---------|--------|
| skill | 调了 Skill | 直接透传答案 | Skill 已经高质量生成，再合成会丢信息 |
| synthesis | 只调了 Tool | 最后做一次合成 | 原始数据需要 LLM 组织语言 |
| direct | 没调任何东西 | 用 LLM 直接回答 | 闲聊不需要检索 |

> 💡 **踩坑故事**：最初所有路径都让 Agent 二次生成，发现 Skill 的引用标注被丢了，token 也白白浪费。

---

### Slide 9-10 — Skill 层详解（7 min）⭐ 全场 Wow 点

**展示一个真实的 SKILL.md 文件**：

```markdown
---
name: compare
description: "对比两个或多个概念/技术/方案的异同。"
tools:
  - search_knowledge
parameters:
  type: object
  properties:
    query:
      type: string
      description: "对比的主题"
    entities:
      type: array
      items: { type: string }
      description: "要对比的对象列表"
---

# Compare Skill

你的任务是对比分析多个对象。

## 执行步骤
1. 从参数取出 entities 列表
2. 对每个 entity，调用 search_knowledge 分别检索
3. 汇总所有检索结果
4. 生成 Markdown 对比表格，标注来源
```

**关键卖点**：
- 改流程？改 Markdown 即刻生效，不用编译部署
- 加新 Skill？建目录 + 写 Markdown，系统自动发现
- 零代码 = 非开发者也能理解/参与 Skill 编写

> 💡 **Live demo**：当场改一行 SKILL.md 指令，演示效果立刻变化。

---

### Slide 11 — Tool 层速览（2 min）

| Tool | 干什么 |
|------|--------|
| search_knowledge | 混合检索 |
| get_document | 查文档元数据 |
| get_chunk | 查文本块+父级上下文 |
| list_documents | 列文档 |
| summarize_text | LLM 摘要 |

> 不多讲，快速过。

---

### Slide 12-13 — 混合检索系统（8 min）

**为什么不只用向量搜索？**

```
问题 A："第39条规定" 
  → 向量搜索可能返回"第40条"（语义近但错了）
  → 关键词搜索精确命中 ✓

问题 B："怎么用 Docker 部署"
  → 文档写的是"如何使用容器化部署"
  → 关键词搜索搜不到 ✗
  → 向量搜索语义匹配 ✓

结论：两个都要，取长补短
```

**检索流水线**（见下方流程图）

**Parent-Child 分块**：

```
Parent（1200 token，无向量）
  ├── Child 1（300 token，✓ 向量）
  ├── Child 2（300 token，✓ 向量）
  └── Child 3（300 token，✓ 向量）
```

检索命中 Child → 返回 Parent（完整上下文）
> 类比：查字典——关键词找到页码（Child），翻开读整页（Parent）

---

### Slide 14 — 数据入库（3 min）

```
文件上传 → nanoid 重命名 → fileHash 查重
  → BullMQ 异步队列 → HTTP 立即返回"已接收"
  → 后台 Worker：Parse → Chunk → Embed
  → 状态更新 ready
```

> 重点讲异步设计：入库要几分钟，不能让 HTTP 阻塞。

---

### Slide 15 — 技术选型一览（3 min）

展示选型表，**不要逐个讲**，强调一个原则：

> **能用一个系统就不开两个**：PostgreSQL 同时搞定关系数据 + 向量检索 + 全文检索。
> 不需要额外部署 Qdrant / Elasticsearch，降低运维复杂度。

---

### Slide 16 — 设计亮点总结（2 min）

1. **Skill = Markdown**：零代码扩展，改文件即刻生效
2. **无硬编码路由**：LLM 自己判断用哪个 Skill，新增自动发现
3. **三终止路径**：每条路径都是最优选择，不浪费 token
4. **一个 PG 搞三件事**：降低运维复杂度
5. **Hook 异常隔离**：旁路不挂主流程

---

### Slide 17 — 未来规划 + Q&A（5 min）

| 阶段 | 重点 |
|------|------|
| Phase 2 | PDF/Word 解析、多知识库、RBAC 权限 |
| Phase 3 | MCP 协议、GraphRAG、评估框架 |

---

---

## 流程图

### 图 1：整体架构

```mermaid
graph TB
    User["👤 用户"]
    
    subgraph HTTP["HTTP / WebSocket 入口"]
        Ingest["POST /ingest<br/>文件上传"]
        Query["WS /ws/query<br/>实时问答"]
        Docs["GET /documents<br/>文档管理"]
    end

    subgraph Agent["Agent 层（LLM 自主决策）"]
        SP["System Prompt<br/>描述所有 Skill + Tool"]
        Loop["Agent Loop<br/>THINK → CALL → OBSERVE → DECIDE"]
        Term{终止路径判断}
        SkillPath["skill 路径<br/>Skill 答案直接透传"]
        SynthPath["synthesis 路径<br/>LLM 合成生成"]
        DirectPath["direct 路径<br/>LLM 直接回答"]
    end

    subgraph Skill["Skill 层（纯 Markdown 驱动）"]
        QA["qa<br/>事实问答"]
        Compare["compare<br/>对比分析"]
        Summary["summary<br/>主题摘要"]
        Search["search<br/>原文检索"]
        Chat["chat<br/>闲聊"]
        Multihop["multihop<br/>多跳推理"]
        Executor["SkillExecutor<br/>mini Agent Loop"]
    end

    subgraph Tool["Tool 层（原子操作）"]
        SearchTool["search_knowledge<br/>混合检索"]
        GetDoc["get_document<br/>查文档"]
        GetChunk["get_chunk<br/>查文本块"]
        ListDoc["list_documents<br/>列文档"]
        SummText["summarize_text<br/>LLM 摘要"]
    end

    subgraph Data["Data 层"]
        PG["PostgreSQL 16"]
        PGVec["pgvector<br/>向量检索 (HNSW)"]
        PGTsv["tsvector<br/>全文检索 (GIN)"]
        Redis["Redis<br/>BullMQ 队列"]
    end

    User --> Ingest
    User --> Query
    User --> Docs
    
    Ingest --> Queue["BullMQ Queue"]
    Queue --> Worker["Worker: Parse → Chunk → Embed"]
    Worker --> PG

    Query --> SP --> Loop
    Loop -->|调用 Skill| Executor
    Loop -->|调用 Tool| Tool
    Loop -->|直接回答| DirectPath

    Executor -->|读 SKILL.md 指令| Skill
    Executor -->|调用 Tool| Tool
    Executor --> SkillPath

    Tool --> SearchTool
    SearchTool --> RRF["RRF 融合"]
    RRF --> PGVec
    RRF --> PGTsv
    SearchTool --> Reranker["Reranker 精排"]
    Reranker --> SynthPath

    Term --> SkillPath
    Term --> SynthPath
    Term --> DirectPath

    SkillPath --> User
    SynthPath --> User
    DirectPath --> User

    Docs --> PG
    Term --> PG
```

---

### 图 2：Agent Loop 详解

```mermaid
flowchart TD
    Start(["用户提问"]) --> BuildSP["构建 System Prompt<br/>列出所有 Skill + Tool 的描述和参数"]
    BuildSP --> CallLLM["调用 LLM<br/>传入 messages + function definitions"]
    
    CallLLM --> HasToolCalls{"LLM 返回了<br/>function call?"}
    
    HasToolCalls -->|"否（content）"| Direct["✅ direct 路径<br/>用 LLM 的 content 作为答案"]
    
    HasToolCalls -->|"是"| ForEach["遍历每个 tool_call"]
    
    ForEach --> IsSkill{"是 Skill<br/>还是 Tool?"}
    
    IsSkill -->|"Skill"| ExecSkill["SkillExecutor 执行<br/>读 SKILL.md 指令 → mini loop → 生成答案"]
    ExecSkill --> AddSummary["给 LLM 发摘要<br/>（不截断原始内容）"]
    AddSummary --> TrackSkill["记录 skillResult"]
    
    IsSkill -->|"Tool"| ExecTool["执行 Tool<br/>返回原始结果"]
    ExecTool --> AddResult["给 LLM 发完整结果"]
    
    TrackSkill --> MaxIter{"超过最大迭代？"}
    AddResult --> MaxIter
    
    MaxIter -->|"否"| CallLLM
    MaxIter -->|"是"| HasSkillResult{"有 skillResult?"}
    
    HasSkillResult -->|"是"| SkillTerm["✅ skill 路径<br/>透传 Skill 的 answer"]
    HasSkillResult -->|"否"| Synth["✅ synthesis 路径<br/>LLM 基于所有 Tool 结果合成答案"]
    
    Direct --> Return(["返回 QueryResult<br/>含 answer + citations + steps + termination"])
    SkillTerm --> Return
    Synth --> Return

    style Start fill:#4CAF50,color:#fff
    style Return fill:#4CAF50,color:#fff
    style Direct fill:#2196F3,color:#fff
    style SkillTerm fill:#FF9800,color:#fff
    style Synth fill:#9C27B0,color:#fff
```

---

### 图 3：混合检索流程

```mermaid
flowchart LR
    Query(["🔍 Query"]) --> Embed["Embedding<br/>文本 → 1024 维向量"]
    Query --> Tokenize["分词<br/>tsvector 分词器"]

    Embed --> Dense["Dense 检索<br/>pgvector HNSW<br/>余弦相似度"]
    Tokenize --> Sparse["Sparse 检索<br/>tsvector GIN<br/>关键词匹配"]
    
    Dense -->|"top K×3"| RRF["RRF 融合<br/>score = Σ 1/(60+rank)<br/>只看排名不看分数"]
    Sparse -->|"top K×3"| RRF

    RRF -->|"top K×3"| Reranker["Reranker 精排<br/>Cross-Encoder<br/>query+doc 拼一起算相关度"]
    
    Reranker -->|"top K"| Dedup["Parent 去重<br/>多个 Child 命中同一个 Parent<br/>只保留一份"]
    
    Dedup --> Expand["展开 Parent 上下文<br/>返回完整文本块"]
    Expand --> Result(["📋 最终结果"])
    
    style Query fill:#4CAF50,color:#fff
    style Result fill:#4CAF50,color:#fff
    style Dense fill:#2196F3,color:#fff
    style Sparse fill:#FF9800,color:#fff
    style RRF fill:#9C27B0,color:#fff
    style Reranker fill:#E91E63,color:#fff
```

---

### 图 4：数据入库流水线

```mermaid
flowchart TD
    Upload(["📁 文件上传<br/>multipart/form-data"])
    
    Upload --> Sanitize["路径安全处理<br/>nanoid 重命名（防路径遍历）"]
    Sanitize --> FileHash["计算 fileHash"]
    FileHash --> DupCheck{"已存在相同<br/>fileHash?"}
    
    DupCheck -->|"是"| DupReturn(["返回：文档已存在"])
    
    DupCheck -->|"否"| CreateDoc["创建 Document<br/>status = pending"]
    CreateDoc --> Enqueue["丢进 BullMQ 队列"]
    Enqueue --> HTTPReturn(["HTTP 立即返回<br/>202 Accepted"])
    
    Enqueue -.->|"异步"| Worker
    
    subgraph Worker["后台 Worker（BullMQ）"]
        Parse["Parse 阶段<br/>文件 → 纯文本<br/>TXT / PDF / Markdown"]
        Parse --> UpdateParse["更新 status = chunking<br/>记录 IngestJob"]
        
        UpdateParse --> Chunk["Chunk 阶段<br/>递归结构化切分<br/>Parent(1200t) + Child(300t)"]
        Chunk --> UpdateChunk["更新 status = embedding<br/>记录 IngestJob"]
        
        UpdateChunk --> Embed["Embed 阶段<br/>批量向量化 (BGE-M3)<br/>TTL/LRU 缓存"]
        Embed --> UpdateEmbed["更新 status = ready<br/>记录 IngestJob"]
    end

    Parse -.->|"失败"| Failure["status = failed<br/>记录 errorMsg"]
    Chunk -.->|"失败"| Failure
    Embed -.->|"失败"| Failure

    style Upload fill:#4CAF50,color:#fff
    style HTTPReturn fill:#2196F3,color:#fff
    style DupReturn fill:#FF9800,color:#fff
    style Failure fill:#f44336,color:#fff
```

---

### 图 5：Skill 执行机制

```mermaid
flowchart TD
    AgentCall(["Agent 调用 Skill<br/>例: compare({query, entities})"]) 
    
    AgentCall --> Load["从 SkillRegistry<br/>取出 Skill 定义"]
    
    Load --> Split{"SKILL.md 解析"}
    Split --> Metadata["frontmatter<br/>name, description<br/>tools 白名单<br/>parameters JSON Schema"]
    Split --> Instructions["正文<br/>执行步骤指令"]
    
    Metadata --> AllowedTools["确定可用 Tool 白名单<br/>例: [search_knowledge]"]
    Instructions --> SystemPrompt["作为 System Prompt"]
    
    SystemPrompt --> MiniLoop["Mini Agent Loop"]
    AllowedTools --> MiniLoop
    
    subgraph MiniLoop["SkillExecutor（Mini Agent Loop）"]
        CallLLM["LLM 读指令"]
        CallLLM --> CallTool["按步骤调 Tool"]
        CallTool --> Observe["看 Tool 结果"]
        Observe --> Done{"按指令<br/>完成了?"}
        Done -->|"否"| CallLLM
        Done -->|"是"| Generate["生成最终答案<br/>（带引用标注）"]
    end
    
    Generate --> Result(["SkillResult<br/>{ answer, citations, toolCalls }"])
    Result --> Transparent(["Agent 直接透传<br/>不二次生成"])
    
    style AgentCall fill:#4CAF50,color:#fff
    style Transparent fill:#FF9800,color:#fff
    style MiniLoop fill:#f5f5f5,stroke:#999
```

---

### 图 6：一次查询的完整数据流

```mermaid
sequenceDiagram
    actor User as 👤 用户
    participant WS as WebSocket
    participant Agent as Agent
    participant LLM as LLM (GPT-4o)
    participant SE as SkillExecutor
    participant Tool as search_knowledge
    participant DB as PostgreSQL

    User->>WS: { question: "对比 Docker 和 K8s" }
    WS->>Agent: 解析 + zod 校验

    Note over Agent: 构建 System Prompt<br/>列出 6 个 Skill + 5 个 Tool

    Agent->>LLM: chat(messages, functionDefs)
    LLM-->>Agent: tool_call: compare({query, entities: ["Docker","K8s"]})

    Note over Agent: 是 Skill → 交给 SkillExecutor

    Agent->>SE: execute(compare/SKILL.md, [search_knowledge])
    
    SE->>LLM: mini loop: 读 SKILL.md 指令
    LLM-->>SE: tool_call: search_knowledge({query: "Docker 微服务"})
    SE->>Tool: search("Docker 微服务")
    
    Tool->>DB: Dense 检索 (pgvector)
    Tool->>DB: Sparse 检索 (tsvector)
    DB-->>Tool: 两路结果
    Tool->>Tool: RRF 融合 + Reranker 精排
    Tool-->>SE: top K chunks

    SE->>LLM: tool_call: search_knowledge({query: "K8s 微服务"})
    SE->>Tool: search("K8s 微服务")
    Tool->>DB: Dense + Sparse
    DB-->>Tool: 两路结果
    Tool->>Tool: RRF + Reranker
    Tool-->>SE: top K chunks

    SE->>LLM: 两组检索结果
    LLM-->>SE: 生成 Markdown 对比表格 + [1][2] 引用

    SE-->>Agent: SkillResult { answer: "| 维度 | Docker | K8s |..." }

    Note over Agent: termination: "skill"<br/>直接透传，不二次生成

    Agent-->>WS: { answer, citations, steps, termination: "skill" }
    WS-->>User: 📋 对比表格 + 引用来源
```

---

### 图 7：Parent-Child 分块策略

```mermaid
flowchart TD
    Doc(["📄 原始文档"])
    
    Doc --> Level1["第一级：Parent 切分<br/>每块 1200 token<br/>对齐段落边界"]
    
    Level1 --> P1["Parent 1<br/>1200 token<br/>❌ 无向量"]
    Level1 --> P2["Parent 2<br/>1200 token<br/>❌ 无向量"]
    Level1 --> P3["Parent 3<br/>1200 token<br/>❌ 无向量"]
    
    P1 --> C1["Child 1a<br/>300 token ✅ 向量"]
    P1 --> C2["Child 1b<br/>300 token ✅ 向量"]
    P1 --> C3["Child 1c<br/>300 token ✅ 向量"]
    P1 --> C4["Child 1d<br/>300 token ✅ 向量"]
    
    P2 --> C5["Child 2a<br/>300 token ✅ 向量"]
    P2 --> C6["Child 2b<br/>300 token ✅ 向量"]
    P2 --> C7["Child 2c<br/>300 token ✅ 向量"]
    P2 --> C8["Child 2d<br/>300 token ✅ 向量"]
    
    Search(["🔍 搜索 Query"]) --> Match["向量匹配命中<br/>Child 2b"]
    Match --> Return["返回 Parent 2<br/>完整 1200 token 上下文"]
    
    style Doc fill:#4CAF50,color:#fff
    style Search fill:#2196F3,color:#fff
    style Match fill:#FF9800,color:#fff
    style Return fill:#FF9800,color:#fff
```

---

## 时间分配速查

| 段落 | 时长 | 形式 |
|------|------|------|
| 封面 + 开场 | 1 min | Slide |
| **Demo** | 5 min | **现场演示** |
| 痛点 + 传统 RAG 对比 | 6 min | Slide |
| 分层架构总览 | 3 min | Slide + 架构图 |
| **Agent 层** | 7 min | Slide + 流程图 |
| **Skill 层** ⭐ | 7 min | **现场改 MD** + 流程图 |
| Tool 层 | 2 min | Slide |
| **混合检索** | 8 min | Slide + 流程图 |
| 数据入库 | 3 min | Slide + 流程图 |
| 技术选型 | 3 min | Slide（快速过表） |
| 设计亮点总结 | 2 min | Slide |
| 未来 + Q&A | 5 min | Slide + 讨论 |
| **合计** | **~52 min** | 留余量压缩到 45 |
